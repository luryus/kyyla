<reactiveui:ReactiveWindow x:Class="Kyyla.AbsenceWindow"
        x:TypeArguments="viewModel:AbsenceViewModel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:reactiveui="http://reactiveui.net"
        xmlns:viewModel="clr-namespace:Kyyla.ViewModel"
        mc:Ignorable="d"
        Topmost="True"
        Style="{DynamicResource WindowStyle}"
        Title="Absence Times" Height="450" Width="800">
    <Grid>
        <ListBox x:Name="listBox" ItemsSource="{Binding Data}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="viewModel:AbsenceViewModel.RowViewModel">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center"
                                HorizontalAlignment="Left">
                        <Label Content="{Binding StartTime}" />
                        <Label Content="{Binding EndTime}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</reactiveui:ReactiveWindow>
